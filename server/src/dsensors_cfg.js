
const cfg = {
  postgres_user: 'postgres',
  postgres_password: 'password',
  postgres_host: 'host',
  postgres_port: 'port',
  postgres_dbname: 'dbname',
  postgres_min: 0,
  postgres_max: 1,
  postgres_schema: 'dsensors',
  certs: '/path_to_certs/certs/',
  project_dir: '/dsensors'
};

module.exports = {
 cfg
};